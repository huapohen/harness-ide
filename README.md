# Harness IDE

轻量 macOS IDE：AppKit 原生窗口、系统 WKWebView、Node.js 本地服务，文本编辑器使用 CodeMirror 6。插件设计参考 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)。没有 Electron、Monaco 或 VS Code 扩展宿主。

## 启动与开发

本机应用安装在 `/Applications/Harness IDE.app`，可通过 Finder 或 Spotlight 启动。应用包包含 Node 和运行资源，不要求另外安装 Node/npm。首次启动会把示例复制到 `~/Library/Application Support/Harness IDE/example`。通过 File → Open Folder 或 Remote Explorer → Open Local Folder 切换工作区。

从源码开发需要 Node.js 22+；macOS 打包另需 Xcode Command Line Tools。

```sh
npm ci

# 构建独立 macOS 应用：build/Harness IDE.app
npm run app

# 为已安装且支持热更新的应用发布修改
npm run hot

# 持续监听源码并发布热更新
npm run hot:watch

# 浏览器模式：打开终端输出的完整本机 URL
npm run build
npm start

# 指定浏览器模式工作区
WORKSPACE=/absolute/path/to/project npm start

# 自动化测试
npm test
```

`npm run dev` 是构建前端后监视后端的开发模式，后端重启会影响该实例的终端任务。日常修改已运行应用的界面优先用 `npm run hot`。服务只监听 `127.0.0.1`，每次启动生成访问令牌。

## 界面与文件

- 左侧目录树保留层级缩进、不显示竖向引导线，支持文件夹滚动固定、文件操作和工作区搜索。右键菜单提供新建文件、文件夹、刷新及 **Collapse Folders in Explorer**；顶部四个操作按钮已移除。
- 最左侧功能栏支持排序；设置按钮固定在最下方，默认隐藏，悬停或键盘聚焦时显示。
- View 菜单控制功能栏、左侧栏、右侧栏、状态栏和标题栏。右侧栏及编辑器分屏边界可拖动调整；布局配置持久化。
- 文件与终端共享标签栏。固定标签用爱心替换关闭按钮，点击爱心取消固定。未保存文件用圆点替换关闭按钮，标签不会因此变宽。
- 从 Finder 拖文件到中央文件区会打开原文件；拖到目录树会复制进工作区。
- 文本使用 CodeMirror 6 和 TextMate 语法高亮，支持 SSH config、SQL 及常见代码。提供查找、替换、正则、保持大小写和全部匹配项选择。
- 所有文本编辑器均启用作用域吸顶（最多五行），保留源码颜色、缩进和行号，支持横向滚动同步、点击跳转和编辑后更新。Markdown 按标题层级识别；代码与配置文件按结构识别，详见下表。Markdown 预览支持标题目录和代码块语法高亮；表格采用统一雾灰底色及暗色网格。代码块和引用使用灰色背景。
- 支持图片查看与编辑、PDF 阅读、受限 HTML 预览；CSV 可编辑或按表格查看。
- Word、PowerPoint、Excel 等 Office 文件通过 LibreOffice 转换为 PDF 预览，不提供 Office 原生编辑。此功能需要可用的 LibreOffice，可通过 `HARNESS_SOFFICE` 指定程序路径。
- 设置中可分别配置编辑器、侧栏、底部终端字号，另有字体、目录树和 Markdown 文字颜色、主题等选项。
- 文件保存前检查版本，防止覆盖外部修改。未保存文件关闭时可选择保存、不保存或取消。运行任务的终端受到关闭保护。

## 编辑器吸顶范围

[VS Code Sticky Scroll](https://code.visualstudio.com/docs/editing/getting-started/userinterface#sticky-scroll) 使用大纲、折叠范围和缩进模型逐级回退，并非只支持 Markdown、Python。Harness 的每个文本编辑器（含分屏）都挂载吸顶功能；没有可识别的嵌套结构时不显示空栏。

| 文件类型 | 识别结构 |
| --- | --- |
| Markdown | ATX / Setext 标题层级，排除围栏代码块中的标题 |
| Python / PYw | 类、同步或异步函数、条件、循环等缩进块；支持跨行参数，跳过多行字符串和注释 |
| JS / TS / JSX / TSX、C / C++ / C#、Java、Go、Rust、Swift、Kotlin、PHP 等 | 花括号、数组和缩进块；独占一行的花括号尽量关联前一行声明 |
| JSON / JSONC / JSONL、CSS / SCSS / Less | 对象、数组、样式块和缩进结构 |
| HTML / XML / SVG / Vue / Svelte、JSX / TSX | 嵌套标签，排除注释和自闭合标签 |
| Shell / Bash / Zsh、Ruby、SQL、Lua | 关键字块以及缩进结构 |
| YAML、INI、TOML、SSH config | 缩进、节标题或 Host / Match 分组 |
| 其他文本文件 | 通用缩进回退，支持显式 region / endregion 区域 |

吸顶从原文复用语法高亮，未引入另一套颜色。示例见 [sticky-scroll-demo.py](example/sticky-scroll-demo.py)。结构识别是轻量规则，不是完整语言服务器；复杂嵌入语言、模板和特殊语法可能与 VS Code 扩展的大纲结果不同。当前没有 VS Code 扩展宿主，因此不宣称兼容所有第三方大纲提供者。

## 快捷键与本地配置

**仓库直接跟踪 [user/keybindings.json](user/keybindings.json)**，这是本机使用的快捷键配置，包含定制的单独 Shift+字母绑定。完整按键表及适用范围见 [KEYBINDINGS.md](KEYBINDINGS.md)。

| 快捷键 | 功能 |
| --- | --- |
| Command+Shift+E / Command+B | 显示 / 隐藏左侧栏 |
| Option+Command+B | 显示 / 隐藏右侧栏 |
| Command+A | 显示 / 隐藏底部终端 |
| Command+Shift+Z | 终端最大化 / 恢复 |
| Command+E | 新建终端 |
| Command+S / Control+S | 保存当前文件 |
| Command+W | 关闭当前未固定标签 |
| Shift+Option+W / Command+Shift+W | 关闭所有未固定标签 |
| Command+3 | 固定 / 取消固定标签 |
| Command+D / Command+Shift+2 / Command+Q | Markdown、HTML 源码 / 预览切换（Command+Q 仅在此类文件且终端未聚焦时生效） |
| Command+Shift+P | 命令面板 |
| Command+, | 键盘快捷键设置 |

`Command+A` 和 `Command+Shift+Z` 是本项目的定制绑定，分别用于终端显示和最大化；全选用 `Control+A`，重做用 `Control+Shift+Z` 或 `Command+Y`。单独 Shift+字母会在文本编辑器中执行对应命令，占用这些大写字母的输入组合，可在设置中改绑。

默认读取 `~/.hot_plugging/user/keybindings.json`，可用 `HARNESS_SETTINGS_DIR` 指定其他配置目录。若将仓库克隆到 `~/.hot_plugging`，仓库文件就是实际配置；克隆到其他位置时，应把该 JSON 放到实际配置目录才会生效，注意保留已有个人修改。

设置 → 键盘快捷键支持搜索、录制、修改和打开 JSON。磁盘配置每两秒重新读取，无需重启。JSON 是完整绑定数组，不与默认值自动合并；校验失败会保留上一次有效配置。配置不存在时由 [shared/keybindings.js](shared/keybindings.js) 创建默认值。“恢复默认”也使用这份源码默认配置。

此文件纳入 Git 后，修改快捷键会产生 Git 差异，拉取更新时需要处理可能的冲突。`user/` 中的布局、插件状态等其他配置仍不上传；根目录的 `keybindings.json` 副本也仍被忽略。

## Git、SSH 与插件

Git 面板采用紧凑的仓库选择、分支信息与提交区布局；变更按暂存状态分组并显示数量，文件行显示文件名，悬停查看完整路径。支持仓库状态、初始化、diff、暂存、取消暂存和提交。面板内提供 Pull（仅快进）、Push、分支切换和新建分支；Repository Scan 可指定目录及扫描深度，默认根目录、两层。

Remote Explorer 从 `~/.ssh/config` 读取主机，点击主机或加号连接，配置按钮直接打开 SSH config 编辑。远端需要 Python 3，Git 操作还需 Git；文件访问采用 OpenSSH BatchMode，应先在系统终端验证主机指纹和密钥认证。工作区切换会经过标签和终端关闭保护。**目前实现 SSH，尚未实现 Remote Tunnels。**

插件面板支持分类、搜索、启用、禁用、卸载与重新安装；核心插件受依赖保护。禁用保留安装文件，卸载删除独立安装副本；共享依赖和内置发行资源可能保留。本 IDE 不直接加载 VS Code 或 Microsoft Remote 扩展。

## 热更新与资源回收

`npm run hot` 发布到 `~/.hot_plugging/hot-updates` 的独立版本目录，并原子切换当前版本指针。应用每两秒检查更新；在插件页面查看更新状态。

- CSS 原地替换；前端代码更新通过状态迁移恢复标签、未保存内容、选区、编辑历史和分屏，重连原有 PTY。
- 文件系统、Git 和设置后端插件可在现有服务内替换。更新失败时保留或回退原版本。
- 对话框、未完成重命名或状态超过保存上限时延后更新。
- Swift、Node、依赖和核心服务变更属于宿主升级，需要新进程；不能通过替换应用包改变仍在运行的旧进程。
- 语法缓存有大小上限，并清理闲置缓存。旧热更新版本会按年龄和保留规则回收，保护当前、已报告仍在使用的版本及依赖。不会自动删除用户文件或本地历史。
- JavaScript / WebKit 负责垃圾回收；缓存释放不保证系统显示的内存占用立即下降。

可通过 `HARNESS_UPDATES_DIR` 隔离测试发布目录。完整应用退出与热更新不同，不保证退出后终端任务继续运行。

## 架构与验证

主要目录：`native/` 原生窗口，`src/plugins/` 前端插件，`server/` 本地服务和后端插件，`shared/` 共用逻辑，`tests/` 自动化测试。前后端组合分别由 `plugins.config.js` 和 `server/plugins.config.mjs` 管理。插件注册的事件、服务和定时器应在卸载时释放。

`npm test` 覆盖插件生命周期、文件保存冲突、搜索、快捷键、PTY、热更新、缓存回收等；测试数量以当前输出为准。真实 SSH 主机连通性仍需在目标环境验证。

```sh
# 打包后验证迁移运行、服务和终端
node scripts/verify-app.mjs
codesign --verify --deep --strict "build/Harness IDE.app"
```

应用使用系统 WebKit 和 ghostty-web WASM 终端，不是 Ghostty.app 原生 Metal 渲染器；目前未提供完整 LSP、VS Code 扩展宿主或多文件夹工作区。HTML 预览受安全限制，Office 预览依赖外部转换器。本机 macOS 包采用 ad-hoc 签名，未做 Developer ID 公证。第三方资源及许可见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md) 和 `native/licenses/`。

## Git 提交范围

提交源码、依赖锁文件、必要静态资源、测试、文档以及明确共享的 `user/keybindings.json`。忽略其余个人配置、`history/`、`hot-updates/`、`output/`、依赖安装目录、构建产物、应用包、日志、`.env` 和密钥文件；实际用户工作区不随源码上传。

Markdown 文件默认以编辑模式打开。标签右键可选择`Edit Mode` 或 `Preview Mode`；标签栏“+”左侧的折角页面按钮也可切换 Markdown / HTML 模式，悬停或键盘聚焦时显示。切换使用当前内容，保留未保存修改。

目录树单击文件会复用一个临时标签，避免标签持续增长。双击顶部文件标签或按 `Command+3` 可固定；单击只切换，标签重命名暂时禁用。固定、未保存、分屏中的文件不会被临时标签替换。标签右键的 `Edit Mode` / `Preview Mode` 用于切换编辑和预览。

### 退出与恢复文件会话

固定标签不能通过任何 Close 操作关闭，必须先 Unpin；批量关闭只处理未固定标签。正常退出应用不会清空文件标签。重新启动会恢复上次工作区的文件、顺序、固定状态、当前标签、编辑/预览模式及文件分屏；未保存文本和新建文本也会备份，已保存文件重新读取磁盘最新内容。

会话自动写入 `~/.hot_plugging/user/sessions/`（本地私有文件，Git 忽略），变更约 750 毫秒后落盘，正常退出会等待最后一次写入。强制结束或突然断电只能恢复最近已写入的状态，不应作为清空标签的方法。要下次空白，先取消固定并关闭所有文件，再退出。备份超过 23 MB 或写入失败时不会允许正常退出，以免丢失修改。

终端进程不属于文件恢复：运行中任务继续受到退出保护；完整退出后不会自动重跑终端命令。热更新仍保留正在运行的 PTY。

本地瘦身：macOS 安装依赖时会移除 node-pty 的 Windows 预编译组件；会话备份只保存恢复所需的内容与选区，不重复序列化撤销历史。暂时无法读取的文件保留恢复记录，后续启动可重试。旧构建可在独立安装包验证后删除，热更新清理保留当前发行、仍在运行的前后端和插件引用，不额外保留回退包；用户配置不清理；文件历史和会话记录按下述保留规则自动管理。

### 备份自动保留规则

- 文件历史：保留 30 天，每个文件最多 50 个版本；所有工作区合计最多 200 MiB（索引及去重后的内容），超限时先删最旧记录，再删无人引用的内容。每次创建历史后检查，并在启动和每小时巡检。
- 会话：每个工作区仍覆盖保存一份；90 天未读写的非当前工作区记录自动清理。当前工作区、含未保存内容的备份不会自动过期，损坏或无法识别的记录也会保留，避免误删。
- 用户配置不受上述清理影响。包含未保存内容的备份需要先恢复并保存或明确丢弃，才会成为可过期记录。

### Auto Save

File → Auto Save 可勾选或取消，默认关闭。开启后，停止编辑约 1 秒自动保存已命名文件（包括固定标签），关闭后仍可手动保存。开关保存在 `~/.hot_plugging/user/layout.json` 的 `autoSave` 字段，重启后保留。未命名文件需先手动 Save As；需要转换格式的图片不会自动弹出另存为。磁盘内容冲突或写入失败会保留未保存状态，不覆盖外部修改。

图片编辑的「文字」工具直接在图片上显示输入框，拖动「拖动文字」改变位置；下方字号、颜色设置条也可独立拖动。点击「应用」合入图片，可撤销；保存会应用当前文字草稿，未应用草稿也会随会话恢复。尺寸调整位于工具栏内，不遮挡图片。目录树图标再次点击可隐藏/显示当前侧栏。

隐藏标题栏后，顶部留白及标签栏空白区域仍可拖动窗口，双击可缩放窗口；标签与按钮保留各自操作。

设置中的「终端光标颜色」提供粉色（默认）、灰色、蓝色、绿色、黄色、白色预置，也可输入自定义 `#RRGGBB`。立即应用于已打开的终端，保存在 `user/layout.json` 的 `font.cursorColor`，重启和主题切换后保留。光标保持不闪烁、上下居中。

- 文件树和标签栏：Shift 点击连续范围多选，Command 点击逐项多选，Command+Shift 点击追加范围。
- 编辑器 Tab 缩进为 4 个空格（约两个中文字符宽），Shift+Tab 反向缩进。Command+↑/↓ 与 Option+↑/↓ 均移动当前行或选中的多行；仅在文本编辑器聚焦时生效。

- 本地 Explorer 支持 `ln -s` 创建的文件及文件夹链接，包括指向工作区外的目录；名称后紧邻显示9px、低对比度的 `↪` 链接标记，悬停可查看失效链接提示。目录按需展开，链接内文件可编辑保存；删除或重命名链接本身不会删除或重命名目标。

- 文件树始终预留滚动条位置，展开文件夹时不会因滚动条出现而改变内容宽度；关闭浏览器自动滚动锚定，避免展开时位置跳动。

- HTML 预览支持点击本地子 HTML 链接，按当前页面所在目录解析相对路径，并支持页面锚点；读取失败会显示错误。左上角 `←` / `→` 支持返回及前进，无对应历史时置灰；返回后打开新链接会清空前进历史；Markdown／CSV 显示为可读文本，HTTP(S) 链接交给系统浏览器。
- HTML 子页面导航通过隔离沙箱内的专用链接桥接处理，避免原生 WebKit 在禁用脚本的文档中忽略宿主点击监听；本地内联脚本可在隔离沙箱中运行，支持页面分栏切换；不授予页面 IDE 同源权限，也不允许页面脚本直接访问网络。

- Explorer 按工作区路径及 SSH 主机分别保存目录展开状态（包括根目录折叠），重启、热更新及切换工作区后恢复。数据保存在 `~/.hot_plugging/user/explorer/`，不会提交 Git。暂时不可访问的目录保留展开记录，不影响其他目录恢复。

- 右侧可折叠侧栏为独立的本地目录树，右上角悬停显示的独立 File 图标可选择根目录（Open Folder…）、关闭根目录（Close Folder）或切换右侧栏（Toggle Side Bar）；右侧树不再占用工具栏行；展开、选中和滚动位置按根目录保存，重启和热更新后恢复，不改变左侧工作区。支持文件图标、符号链接标记、右键新建/删除/刷新和原地重命名。左右目录树各自使用独立的临时文件标签位置，固定标签继续保留。

- 标签支持触摸板轻点切换文件和终端；轻点抬手与 click 去重，拖动不会误触发选择，关闭按钮和双击固定保持独立。

- 标签轻点或单击切换，双击切换固定／取消固定；标签重命名入口暂时禁用、实现保留。Explorer 双击或选中后按 Enter/F2 可原地重命名（Enter 确认，Escape 取消）。
- 拖动标签到顶部栏可吸附放置，再拖回普通标签栏；支持同栏排序，顶部位置随布局会话保存。标题栏隐藏时，顶部已有标签会保留一行紧凑栏。

- 顶部吸附标签保留自由横向放置位置，重叠时自动错开；空间不足时支持横向滚动，位置随会话保存。

- 顶部吸附拖动不显示边界框，保留落点定位和重叠避让。

- 右侧栏开关移到窗口右上角，悬停或键盘聚焦时显示；相邻排列按钮提供自由（默认）、左对齐、右对齐、居中四种顶部标签布局，设置自动保存。

设置面板按字体与字号、主题与颜色、终端光标、编辑器及快捷键分组，默认折叠，点击分组展开。编辑器右侧大滑块默认宽度为 50px（原 100px），可在“设置 → 编辑器 → 大滑块宽度”调整为 20–200px，实时生效；保存于 `user/layout.json` 的 `font.overviewWidth`，重启和热更新后保留。

文本编辑器支持滚动超过最后一行：滑到底时，最后一行可移到视口顶部，下方保留接近一屏空白；留白随编辑区高度自动调整，适用于 Markdown、Python 等文本文件。

近期回归检查修复了侧栏异步刷新覆盖、Git 提交草稿丢失、快速折叠目录后出现过期内容、顶栏拖动偏移及批量关闭时的固定保护问题。Git 提交草稿在本次运行中按仓库分别保留，刷新、暂存后仍在。检查范围与验证记录见 [近期 UI 回归检查](docs/recent-ui-audit.md)。

全仓库检测记录见 [2026-09-13 全量检测](docs/full-audit-2026-09-13.md)：120 项测试通过，并完成 Swift 类型检查、安装包独立运行验证与依赖安全审计。同一文件并发保存（含链接别名）会拒绝过期版本；历史恢复写入前再次校验版本；原地重命名期间热更新等待完成。

侧栏滚动条轨道使用侧栏背景，避免 macOS WebKit 在透明滚动条配色下出现白条；目录行与按钮使用手形指针，文本编辑区和重命名输入框使用文本指针，侧栏分隔线使用左右调整指针。

右侧 File 包含与原生 File 相同的新建、打开、最近打开、保存、另存为、全部保存、自动保存选项，并提供 Close Folder 和 Toggle Side Bar。右侧最近记录、自动保存开关独立保存；保存操作只处理右侧打开的标签，当前选中左侧标签时右侧 Save/Save As 禁用。

右侧文件夹图标继续悬停显示。右侧目录树空白处菜单与左侧包含相同的新建、Finder 定位、集成终端、共享、粘贴、复制路径、刷新和折叠选项，并额外提供 Close Folder、Toggle Side Bar；剪贴操作使用右侧自己的目录上下文。

右侧目录树回归修复：根目录切换/关闭会作废尚未确认的旧目录操作；多选删除包含全部选中项并去除父子重复；新建、粘贴自动展开祖先目录。左右外部文件标签标识独立，热更新/重启后保留归属。左侧连接 SSH 时，右侧本地目录的集成终端暂不可用，会明确提示，避免把本地路径发送到远程终端。

标签栏右侧的新建、左右切分、上下切分三个按钮采用紧凑布局：每个宽 24px，无额外间距，图标大小和功能保持不变。

左右目录树的焦点行只保留灰色选中背景，不显示 WebKit 蓝色焦点框或横杠；原地重命名输入框使用普通灰色边框。

左右目录树点击空白处会清空单选/多选与行焦点；右侧清空状态会保存。深色主题的目录选中底色使用低对比灰色 `#2c313a`，与示例 VS Code 选中效果对齐。

标签栏右侧的预览、新建、左右切分、上下切分四个紧凑按钮默认隐藏，鼠标悬停对应按钮或键盘聚焦时显示，位置不发生变化。

右上角排列、独立 File、右侧栏开关在悬停或键盘聚焦时使用较亮的前景色，默认仍隐藏。

左右本地目录树空白处或文件夹右键支持 Add Symbolic Link Here…：选择目标目录后输入链接名称，在当前目录创建软链接，已有同名项不会被覆盖。活动栏图标上移并收紧间距；图标显示菜单中的 Settings 排在 Plugins 后面。

右侧栏分隔线与左侧一样显示为 1px 灰线，保留 5px 透明拖动区域；悬停、拖动和聚焦均不显示粉色高亮。

Light 主题的左右目录树文字使用较深的灰色 `#505763`，提高浅色背景下的可读性；该规则仅限 Light，不影响 One Dark Pro。

设置分组使用细线折叠箭头替代实心三角，折叠朝右、展开朝下，与主题文字颜色协调。

应用界面统一禁用系统焦点轮廓、焦点阴影和强调色焦点横线；设置分组、按钮、输入框及目录树均适用。文本选择本身仍保留高亮，便于编辑。
编辑器右侧概览条也不再绘制蓝色光标位置横线。

Git 面板的 Repository Scan 可配置扫描目录（绝对路径或工作区相对路径）及深度，默认工作区根目录和 2 层，按工作区保存。根目录算 0 层，跳过 .git、node_modules、.venv、venv；超过 10000 个目录明确报错。点击分支名选择本地分支；Pull 使用 --ff-only，Push 使用普通推送，不强推。提交信息提示字降低亮度。
分支下拉菜单提供 Create New Branch…，从当前 HEAD 创建并切换本地分支，不自动推送。

Git 分支名完整显示，长名称自动换行，不使用省略号；点击仍可打开分支菜单。

### 工作区搜索
本地搜索优先使用 ripgrep（`HARNESS_RG` 指定路径，其次本机 VS Code、Homebrew 或 ChatGPT 自带的版本），JSON 流式解析结果；未找到时及 SSH 工作区保留兼容搜索。普通文本、正则、大小写、全词、包含/排除路径、仅打开文件与未保存内容均支持。取消原来的 10,000 文件和累计 64 MB 扫描限制；最多返回 20,000 条匹配，确有更多匹配时明确提示结果截断。单次超时为 30 秒。结果按文件分组、分批渲染，每组先显示 100 条，可点 Show more 加载后续结果。当前不跟随软链接，默认排除 `.git`、`node_modules`、`.harness-trash`；不使用 ignore 文件。SSH 仍使用兼容实现，不承诺本地 ripgrep 的速度。
搜索面板使用紧凑分组布局，文件名、父路径及匹配数分列显示；include / exclude 标签旁的文件夹图标可打开本机目录选择器，自动追加工作区相对目录条件，已有条件不覆盖、同目录不重复。远程目录仍手输；工作区之外的目录不会误加到当前搜索范围。
文件夹筛选按钮紧随 include / exclude 标签。搜索不生成磁盘临时缓存；完成、失败或超时后回收工作线程和 ripgrep 子进程。同一面板启动新搜索会取消旧搜索，清空结果、切换工作区及卸载面板时释放结果引用并取消后台任务；当前可见结果保留供定位及导出使用。
Git Source Control 按需扫描；切换到其他面板、折叠左侧栏或卸载 Git 插件时，主动清空列表 DOM 和提交草稿，取消未完成的只读查询（本地 Git 子进程随之终止）。再次打开重新加载，不后台轮询。扫描目录/深度配置仍保留；已开始的 Pull、Push、Commit 等写操作正常完成，不会因为离开面板被强制中断或完成后抢回侧栏。远程正在执行的单次 SSH 命令会完成或超时，后续扫描停止。

最近一轮全量回归修复了 Git 侧栏从菜单/快捷键重新展开为空白、异步初始化抢回侧栏、Diff 使用可变仓库上下文的问题；搜索清除已退出子进程 PID，限制错误输出缓冲。热更新启动或 CSS 更新完成后立即清理旧发行缓存，保护当前客户端、后端、插件依赖，不额外保留旧回退包；平时仍定期执行维护。
旧包维护也会回收超过一小时、且发布进程已退出的 `.tmp-PID` / `.init-PID` 临时目录，避免失败构建长期占用空间。
热更新不再额外保留三个回退版本；仅保留当前发行和仍在使用的前后端/插件资源；安装基础资源若被宿主引用则保留，确认无引用后才可删除。仅后端更新且 CSS 未变时不重复加载样式，状态显示 Up to date。
左右目录树统一使用细灰色滚动条（8 px 轨道、约 6 px 可见滑块，深色主题 20% / 浅色主题 17% 不透明度），鼠标靠近侧边时显示、离开后约 280 ms 淡出；浅色主题单独降低对比度，保留固定滚动条空间避免目录展开跳动。
Markdown 预览滚动滑块与源码模式使用相同的主题颜色和 75% 显示透明度；仅在滚动或鼠标靠近滚动条时显示，停止滚动 800 ms 且鼠标移开后，以 250 ms 淡出。
图标字体（Codicon、Seti）现在内嵌在构建后的 CSS 中，不再请求安装基础包里的 `/codicon.ttf`、`/seti.woff`；热更新等字体加载完成后再移除旧样式。这样删除闲置基础包副本也不会令图标变成方框。

搜索后端在启用期间持有版本引用，缓存清理保留其延迟启动的 worker 文件；后端替换后释放引用。

Markdown 支持相对/绝对本地路径、HTTP(S) 和引用式链接：源码中 ⌘/Ctrl 点击打开，预览中直接点击打开。两种模式的图片链接均可悬停预览；本地内嵌图片也可显示。图片浮窗关闭时释放临时资源；命令面板提供“Markdown · 显示图片链接预览”。

Markdown 源码与预览切换按源文件段落行号同步阅读位置，保留章节内进度及行内滚动比例。

标签栏“+”左侧提供与相邻按钮视觉尺寸一致的折角页面线框图标，悬停显示，可切换 Markdown / HTML 的预览与源码；其他文件类型不可用。

Markdown / HTML 退出预览或关闭标签时释放图片对象 URL、页面 DOM、HTML 桥接及导航记录，忽略已关闭预览的迟到请求。图片链接解析按文档版本缓存，光标移动不重复序列化整份文件。全量检查结果见 [全量检查报告](docs/full-audit-2026-09-13.md)。

缓存维护会对保留发行中的大型、相同语法资源和后端依赖建立硬链接共享，保留全部访问路径；删除某个版本不会破坏其他版本。该优化只处理不可变发行资源，不处理用户文件。

MacBook 触摸板轻点标签在按下时激活，兼容 WKWebView 偶发的抬起先于按下且缺少 click 的事件顺序；只有实际拖动才捕获指针。搜索、替换和范围输入框支持按住拖选文本，包括 WebKit 拖动时 buttons 为 0 的情况。

设置 → 主题与颜色提供左侧栏、右侧栏、文件区域和终端的独立背景色，可使用选色器或输入 #RRGGBB，实时生效并按 One Dark Pro / Light 分别保存在本机。支持恢复当前主题的背景默认值。深色主题两侧目录树默认统一为比原版略浅、仍比文件区略深的 #252930（含固定目录行与滚动条轨道）；文件和终端默认仍为 #282c34。HTML 页面自行指定的背景、图片及 PDF 文档本身的颜色不被覆盖。

Light 模式下 Markdown 预览正文（含引用和行内代码）使用深灰 #30343b，柔和于纯黑且保持清晰，标题与链接配色不变；终端主题适配分别映射前景和背景，避免深色默认值与 ANSI 黑/白同色时互相覆盖。One Dark Pro 的 Markdown 文字配置保持原样。

终端右键菜单第一项为 Hide：隐藏当前终端视图（分屏时隐藏该组），保留会话、输出及运行任务；点击终端标签或切换底部终端可再次显示。Close 仍结束空闲终端会话，不删除磁盘文件。

macOS 顶部 View → Toggle Terminal 切换底部终端的显示/隐藏，保留会话；没有终端时新建。此原生菜单项需要更新主程序后重新打开应用才会出现。

热更新恢复终端时，先保留原来的行列尺寸完成输出重放，待原分栏布局恢复后再适配尺寸，避免临时放大到编辑区再缩回底部分栏而触发多余的提示符重绘。该修复不清屏、不发送回车，不修改用户输入。

新建本地 zsh 终端时，输入先暂存至 ZLE 行编辑器首次就绪再发送，避免初始化期间 PTY 提前回显字符，导致提示符前残字或空行。使用真实 zsh 延迟启动并立即输入的测试验证字符仅回显一次。此终端后台修复需要重新打开应用后生效。

终端首次绘制时暂不显示尚停留在原点的光标；收到实际光标位置后正常显示，避免启动瞬间在提示符前闪现色块。之后正常回到原点时仍显示光标，不影响清屏或全屏程序。

光标从启动隐藏状态转为显示，或终端切换光标可见性时，会重新绘制文字，避免旧光标所在单元格留下遮挡。

终端输入聚焦隐藏 textarea，外层画布容器不再可编辑，避免启动时原生文字输入落入容器、挤动画布或遮挡提示符首字符。

终端启动光标等待 shell 输入就绪后再显示，避免 zsh 提示符准备阶段的临时光标遮住首字符；连接建立前的键盘输入暂存并在连接后按顺序发送。
启动画面在 shell 就绪并完成首帧后显示；若初始化超过 2 秒，则先显示已有输出，避免遮住启动诊断或交互。已在 macOS 原生窗口连续验证三轮退出、新建并立即输入。

终端顶部和右侧边界贴齐放置 22×18px 紧凑背景框的细线 ×（Hide Terminal）按钮，仅鼠标移到按钮本身或键盘聚焦时显示，只隐藏面板并保留会话，可通过 Toggle Terminal 再次打开。

活动栏右键菜单的图标隐藏状态保存到磁盘布局配置，重启后恢复；首次升级会迁移当前窗口的隐藏选项。

自动换行、界面缩放、主题及分主题背景色保存到磁盘，启动时先恢复再创建界面。原生窗口保存手动调整后的大小和位置，再次启动恢复。

左右资源管理器原地新建文件和目录，Enter 确认、Esc 或失焦取消；文件操作后同步刷新两侧。重命名/移动不再递归读取内容创建历史快照，避免大文件限制阻止目录改名；删除前的历史保护保持原样。

新终端使用当前空闲的最小 t 编号；已退出的编号可复用，隐藏或分屏中的终端仍占用其名称，自定义名称和恢复会话名称保持不变。

终端分屏分隔线为 1px，横向和纵向均保留扩展拖动感应区域。

设置 → 终端可切换环境、用户名和主机名显示，默认关闭；本地 zsh 新终端显示“目录 %”，开启后使用原有 shell 提示符。设置持久化，对新建终端生效，不改用户 shell 配置或停止已有会话。

左右文件树支持拖动文件或文件夹移动到目录，也支持两个本地根目录之间拖动；拖到文件行以其父目录为目标，重名拒绝覆盖，自身子目录拒绝移动。涉及已打开文件时先按现有关闭/保存流程处理，两侧随后刷新；不支持本地与 SSH 之间拖动。

文件树拖放目标随左侧栏当前容器解析，刷新文件树或从搜索返回后仍可拖放；非文件树面板不接收文件移动。

文件树拖动时显示跟随指针的紧凑文件名浮层，并高亮目标行；移动后刷新左侧目录树保留滚动位置，避免跳回顶部。

拖放后的两侧文件树在后台完成读取和构建后再整体替换，避免先清空再逐层出现的闪烁；同侧拖放只刷新一次，并保留滚动位置。

拖放移动采用局部目录协调：仅重读源父目录和目标目录的直接列表，复用未改变的行及已展开子树；其他目录不读取、不重建，左右侧栏按各自根目录同步对应范围。

左右文件树每级缩进为 8px。吸顶使用独立祖先栏（最多 7 行），只显示当前滚动分支的祖先；目录行本身不再逐行 CSS sticky，避免兄弟目录交错、重叠遮挡。点击吸顶行可定位到对应目录。

设置 → Office 文件 → 打开方式，默认「跟随系统默认应用」。本地 Word、Excel、PowerPoint 文件直接交给 macOS 文件关联的应用（如 WPS），不转换、不读取完整文档、不新增 Office 引擎依赖；选择会持久保存。可选已有 LibreOffice 的 IDE 只读预览。远程文件使用系统应用前需先下载。

终端画布在布局调整时按屏幕像素及原生窗口缩放对齐位置，减少底部分栏或分屏落在分数像素时的文字模糊；不缩放位图、不改变字号或终端会话。

终端拖选可见区域的首尾行不会触发自动滚动；只有指针越过终端上下边界才滚动，避免最后两行横向选字时定时滚动干扰选区。

退出应用时，如终端仍有任务或状态无法确认，会列出终端及原因并询问是否继续；取消保留任务，继续会先备份会话再退出。会话保存失败会显示错误，不再静默拒绝退出。

- 终端输入框放行 IME 组合输入，Control+Space / Fn 不编码为终端字符；点击画布后保持输入框焦点。实体输入源切换需由 macOS 配置提供。

- 搜索结果使用独立的细滚动条，可拖动定位；搜索框、筛选项和结果统计保持固定。

- Finder 拖入文件或文件夹后，仅合并目标目录的直接子项；保留两侧目录树的展开状态和当前位置，不重建整棵树。折叠的目标目录保持折叠。

- 中文输入法直接提交的数字和标点通过 input 事件写入终端，拼音组合提交保持单次发送；Finder 导入失败显示具体原因。

- 设置 → 主题与颜色 → 左右侧栏前景色可同步调整两侧目录文字；深色默认值为 #A3AAB7。

- 两侧目录树支持多选文件/文件夹后用 Ctrl/Cmd+C、X、V 或右键菜单批量复制、剪切、粘贴；多选拖动同样移动全部选中项。粘贴直接执行，目标重名会在开始前报错，避免覆盖；有未保存编辑时仍保留保存保护。两侧局部刷新并保持目录位置。
- 设置 → 主题与颜色 → 标签完整名称悬停延迟（毫秒）：默认 300，可设为 0–5000，重开应用保留。

- 外部新建、删除、重命名文件后，两侧已展开目录每秒自动检查直接子项，仅局部更新变化目录；保留展开状态和滚动位置，重命名输入和拖动期间暂缓，折叠目录不递归扫描。
