import {test} from 'node:test';import assert from 'node:assert/strict';import {markdownBlocks,interpolatePosition} from '../src/markdown-progress.js';
test('source positions account for whitespace and fenced code without treating it as headings',()=>{const blocks=markdownBlocks('# One\n\nParagraph\n\n```md\n# Not heading\n```\n\n## Two\n');assert.equal(blocks.find(b=>b.html.includes('<h2>')).line,9);assert.ok(blocks.some(b=>b.html.includes('<pre>')));});
test('block rendering preserves reference links from elsewhere in the file',()=>{assert.ok(markdownBlocks('[photo][p]\n\n[p]: ./image.png\n').some(b=>b.html.includes('href="./image.png"')));});
test('reading progress maps within a section and clamps at document boundaries',()=>{const points=[{line:1,y:0},{line:20,y:400},{line:40,y:1400}];assert.equal(interpolatePosition(900,points,'y','line'),30);assert.equal(interpolatePosition(30,points,'line','y'),900);assert.equal(interpolatePosition(-20,points,'y','line'),1);assert.equal(interpolatePosition(5000,points,'y','line'),40);});
