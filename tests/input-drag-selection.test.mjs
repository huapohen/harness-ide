import {test} from 'node:test';import assert from 'node:assert/strict';import {inputDragSelection} from '../src/input-drag-selection.js';
test('WebKit pointer drags select both directions even when buttons is zero',()=>{const saved={document:globalThis.document,getComputedStyle:globalThis.getComputedStyle},events={};let capture=false;const input={value:'high',offsetWidth:100,scrollLeft:0,selectionStart:0,getBoundingClientRect:()=>({left:10,width:100}),setSelectionRange(a,b,d){this.selection=[a,b,d];},setPointerCapture(){capture=true;},hasPointerCapture:()=>capture,releasePointerCapture(){capture=false;},addEventListener:(n,f)=>events[n]=f};globalThis.document={createElement:()=>({getContext:()=>({measureText:s=>({width:s.length*10})})})};globalThis.getComputedStyle=()=>({font:'12px monospace',paddingLeft:'0',borderLeftWidth:'0',letterSpacing:'0'});try{inputDragSelection(input);const e={button:0,buttons:0,pointerId:1,clientX:55,preventDefault(){}};events.pointerdown(e);events.pointermove({...e,clientX:10});assert.deepEqual(input.selection,[0,4,'backward']);events.pointerup(e);assert.equal(capture,false);events.pointerdown({...e,clientX:10});events.pointermove({...e,clientX:55});assert.deepEqual(input.selection,[0,4,'forward']);events.pointercancel();}finally{Object.assign(globalThis,saved);}});
