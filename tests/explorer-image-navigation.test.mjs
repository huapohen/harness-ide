import test from 'node:test';import assert from 'node:assert/strict';import {imageNavigation} from '../src/explorer-image-navigation.js';
test('rapid image navigation finishes on latest item and preserves tree focus',async()=>{let release;const opened=[],focused=[];const nav=imageNavigation(async path=>{opened.push(path);if(opened.length===1)await new Promise(r=>release=r);});const row=path=>({dataset:{path},isConnected:true,focus:()=>focused.push(path)});const first=nav.select(row('a.jpg'),{});await nav.select(row('b.png'),{});await nav.select(row('c.jpg'),{});release();await first;assert.deepEqual(opened,['a.jpg','c.jpg']);assert.equal(focused.at(-1),'c.jpg');await nav.select(row('d.jpg'),{shiftKey:true});await nav.select(row('readme.md'),{});assert.equal(opened.length,2);nav.dispose();});
