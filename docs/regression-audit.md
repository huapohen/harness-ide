# 全量回归检查（2026-09-13）

范围：全部 Node 测试，包括搜索、Git、双目录树、符号链接、HTML 导航、文件保存与会话、终端、标签拖动、主题、插件和热更新；生产构建与当前 macOS 应用内关键流程验证。

本轮修复：
- Git 页面经菜单/快捷键隐藏后重新显示会重新加载；隐藏状态恢复不保留列表。
- 初始化完成后不抢回已离开的侧栏；Diff 使用原始仓库上下文。
- 取消扫描时不吞掉取消信号；新增取消回归。
- 搜索进程结束后清除 PID 记录，stderr 缓冲限制为 8 KB。
- 热更新完成即触发旧发行清理，跳过周期节流；保留当前版本、活动客户端/后端引用、插件依赖和最近三个版本，新增近期缓存清理回归。

限制：自动测试不能证明不存在任何 bug。真实 SSH 主机、远程认证和网络 Push/Pull 不使用用户仓库做破坏性验证；现有本地临时 Git 测试验证分支、扫描、取消和状态。用户终端任务不因检查或瘦身终止。

补充：发现一个已退出发布进程留下的约 6 MB staging 目录；清理器现会回收超过一小时且所属进程已退出的精确命名临时目录，保留仍在发布的目录，并增加相应测试。
