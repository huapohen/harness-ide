# 全量回归检查（2026-09-13）

范围：全部 Node 测试，包括搜索、Git、双目录树、符号链接、HTML 导航、文件保存与会话、终端、标签拖动、主题、插件和热更新；生产构建与当前 macOS 应用内关键流程验证。

本轮修复：
- Git 页面经菜单/快捷键隐藏后重新显示会重新加载；隐藏状态恢复不保留列表。
- 初始化完成后不抢回已离开的侧栏；Diff 使用原始仓库上下文。
- 取消扫描时不吞掉取消信号；新增取消回归。
- 搜索进程结束后清除 PID 记录，stderr 缓冲限制为 8 KB。
- 热更新完成即触发旧发行清理，跳过周期节流；保留当前版本、活动客户端/后端引用、插件依赖，不额外保留旧回退包，新增近期缓存清理回归。

限制：自动测试不能证明不存在任何 bug。真实 SSH 主机、远程认证和网络 Push/Pull 不使用用户仓库做破坏性验证；现有本地临时 Git 测试验证分支、扫描、取消和状态。用户终端任务不因检查或瘦身终止。

补充：发现一个已退出发布进程留下的约 6 MB staging 目录；清理器现会回收超过一小时且所属进程已退出的精确命名临时目录，保留仍在发布的目录，并增加相应测试。

## 2026-09-13 搜索 worker 旧版清理回归

修复缓存维护只保护前端/设置模块、遗漏实际文件后端版本的问题。文件后端在激活与释放时登记版本引用，清理读取引用并保留延迟启动的 worker 及依赖。138 项测试通过，含后端引用释放前后清理测试。补回运行窗口引用的缺失后端资源后，对实际工作区搜索 high 返回 20000 条，耗时约 1.5 秒。新版本 5427cbacf8b6499517cf 已发布；本次 UI 操作未能确认窗口热更新状态，不能声称现场验证完成。

## 2026-09-13 Markdown 链接与图片

141 项测试通过，覆盖中文相对路径、绝对路径、引用式链接、代码块排除、远程路径越界限制。原生窗口已加载 aa4b117d4ef699571856：调研报告中的本地 Markdown 链接正确打开，预览图片链接打开 JPEG 编辑器，源码图片浮窗显示成功。预览悬停与源码共享图片加载和回收逻辑；自动操作接口无鼠标悬停方法，未独立验证预览鼠标悬停。

## 2026-09-13 Markdown 阅读进度与独立悬停实测

已在 Harness 原生窗口的 Markdown 预览模式，将指针停在调研报告“外观图”链接上，独立确认工牌图片浮窗显示。第 2 章长表格中段切换源码及返回预览，均保持对应章节位置。新增块级源行映射与双向插值，保留行内比例；144 项测试全部通过。最终窗口已加载 222937108d3405f6406a。清理时保留当前前端及 aa4b117d4ef699571856 文件后端资源。

## 2026-09-13 标签栏预览开关

“+”左侧新增预览按钮，沿用现有悬停隐藏样式，支持 Markdown / HTML，非支持文件禁用。144 项测试通过；原生窗口已加载 4c7c4709c26b2400d4b4，点击新按钮确认从预览切换为源码，状态由 on 变为 off。

预览切换图标按反馈改为文档布局线框，移除眼睛造型；交互保持不变。

预览切换按钮再次调整为折角页面加代码括号图标，保留悬停显示。
